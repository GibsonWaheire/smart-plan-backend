{
  "name": "smart-saving-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "json-server --watch db.json --port 3000",
    "build": "echo 'No build step required for json-server'"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

